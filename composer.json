{
    "name": "progminer/maintenance-screen",
    "description": "The \"Maintenance mode\" screen library",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "ProgMiner",
            "email": "eridan200@mail.ru"
        }
    ],
    "minimum-stability": "dev",
    "prefer-stable": true,
    "require": {
        "symfony/config": "^4.0",
        "symfony/http-foundation": "^4.0",
        "progminer/util-template-renderer": "dev-master"
    },
    "require-dev": {
        "symfony/yaml": "^4.0",
        "twig/twig": "^2.4"
    },
    "suggest": {
        "symfony/yaml": "To use the yaml file loader",
        "twig/twig": "To use the twig template renderer"
    },
    "autoload": {
        "psr-4": {
            "MaintenanceScreen\\": "lib"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "scripts": {
        "sami-install": [
            "curl -O http://get.sensiolabs.org/sami.phar"
        ]
    }
}
